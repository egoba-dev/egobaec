{% extends '@admin/default_frame.twig' %}

{% set menus = ['plugin', 'category_product_block'] %}

{% block title %}カテゴリー商品ブロック設定{% endblock %}
{% block sub_title %}プラグイン設定{% endblock %}

{% block main %}
<div class="c-contentsArea__cols">
    <div class="c-contentsArea__primaryCol">
        <div class="c-primaryCol">
            {% if error is defined %}
                <div class="alert alert-danger">
                    <strong>エラーが発生しました:</strong> {{ error }}
                </div>
            {% endif %}

            <div class="card rounded border-0 mb-4">
                <div class="card-header">
                    <div class="row">
                        <div class="col-8">
                            <span class="card-title">基本設定</span>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    {% if form %}
                        {{ form_start(form) }}
                        
                        <div class="row">
                            <div class="col-3">
                                <span>表示設定</span>
                            </div>
                            <div class="col mb-2">
                                <div class="row mb-3">
                                    <div class="col-4">
                                       {{ form_widget(form.display_count) }}
                                        {{ form_errors(form.display_count) }}
{% if form.display_count.vars.help is defined %}
    <small class="form-text text-muted">{{ form.display_count.vars.help }}</small>
{% endif %}                                    </div>
                                    <div class="col-auto align-self-center">件を表示</div>
                                </div>
                                
                                <div class="row mb-3">
                                    <div class="col-2">
                                        {{ form_widget(form.columns) }}
                                        {{ form_errors(form.columns) }}
                                    </div>
                                    <div class="col-auto align-self-center">列 ×</div>
                                    <div class="col-2">
                                        {{ form_widget(form.rows) }}
                                        {{ form_errors(form.rows) }}
                                    </div>
                                    <div class="col-auto align-self-center">行で表示</div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="row">
                            <div class="col-3">
                                <span>表示オプション</span>
                            </div>
                            <div class="col mb-2">
                                <div class="form-check mb-2">
                                    {{ form_widget(form.show_category_tags) }}
                                    {{ form_label(form.show_category_tags, null, {'label_attr': {'class': 'form-check-label'}}) }}
                                </div>
                                
                                <div class="form-check mb-2">
                                    {{ form_widget(form.show_product_list_button) }}
                                    {{ form_label(form.show_product_list_button, null, {'label_attr': {'class': 'form-check-label'}}) }}
                                </div>
                            </div>
                        </div>

                        <div class="c-conversionArea">
                            <div class="c-conversionArea__container">
                                <div class="row justify-content-between align-items-center">
                                    <div class="col-6">
                                        <div class="c-conversionArea__leftBlockItem">
                                            <a class="c-baseLink" href="{{ url('admin_homepage') }}">
                                                <i class="fa fa-backward" aria-hidden="true"></i>
                                                <span>ホーム</span>
                                            </a>
                                        </div>
                                    </div>
                                    <div class="col-6">
                                        <div id="ex-conversion-action" class="row align-items-center justify-content-end">
                                            <div class="col-auto">
                                                <button class="btn btn-ec-conversion px-5" type="submit">登録</button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        {{ form_end(form) }}
                    {% else %}
                        <div class="alert alert-warning">
                            設定フォームを読み込めませんでした。
                        </div>
                    {% endif %}
                </div>
            </div>
            
            <div class="card rounded border-0 mb-4">
                <div class="card-header">
                    <span class="card-title">使用方法</span>
                </div>
                <div class="card-body">
                    <h5>ブロック配置</h5>
                    <p>コンテンツ管理 > ブロック管理から「カテゴリー商品ブロック」を各ページに配置してください。</p>
                    
                    <h5>機能</h5>
                    <ul>
                        <li>カテゴリータグをクリックすると、該当カテゴリーの商品が表示されます</li>
                        <li>商品をクリックすると商品詳細ページに移動します</li>
                        <li>「商品一覧を見る」ボタンでカテゴリー商品一覧ページに移動します</li>
                        <li>レスポンシブ対応でモバイル・タブレット・PCで最適表示されます</li>
                    </ul>
                    
                    {% if Config %}
                        <h5>現在の設定</h5>
                        <ul>
                            <li>表示件数: {{ Config.displayCount }}件</li>
                            <li>表示形式: {{ Config.columns }}列 × {{ Config.rows }}行</li>
                            <li>カテゴリータグ: {{ Config.showCategoryTags ? '表示' : '非表示' }}</li>
                            <li>商品一覧ボタン: {{ Config.showProductListButton ? '表示' : '非表示' }}</li>
                        </ul>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}